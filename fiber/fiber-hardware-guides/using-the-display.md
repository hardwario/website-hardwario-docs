---
title: Using the Display
---

# Using the Display

**FIBER only** — FIBER Lite has no display (see [What's Different](/fiber/fiber-lite/introduction#whats-different)).

This guide covers drawing to FIBER's backlit LCD from the Linux side.

:::danger

**Content pending.** This page needs the display's exact interface (SPI + reset/DC pins, an I2C
address, or a `/dev/fbN` framebuffer device) before the real procedure can be written.

:::
